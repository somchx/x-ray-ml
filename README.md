# 🫁 Chest X-Ray Analysis Web Application

เว็บแอปพลิเคชันสำหรับวิเคราะห์ภาพเอ็กซเรย์ปอดด้วย AI เพื่อตรวจจับความผิดปกติ (Pneumonia)

## ✨ Features

- 📤 อัปโหลดภาพเอ็กซเรย์ปอด (รองรับ .jpg, .jpeg, .png)
- 🤖 วิเคราะห์ด้วย Deep Learning Model
- 📊 แสดงผลการวิเคราะห์พร้อม Confidence Score
- 🎨 UI ที่สวยงามและใช้งานง่าย
- 🔄 รองรับ Drag & Drop

## 🚀 การติดตั้ง

### 1. ติดตั้ง Dependencies

```bash
pip install -r requirements.txt
```

หรือติดตั้งทีละตัว:

```bash
pip install flask tensorflow numpy Pillow werkzeug
```

### 2. เตรียม Model

ตรวจสอบว่ามีไฟล์ `my_model.h5` อยู่ในโฟลเดอร์เดียวกับ `app.py`

## 🎮 การใช้งาน

### เริ่มเซิร์ฟเวอร์

```bash
python app.py
```

### เปิดเว็บบราวเซอร์

ไปที่: `http://localhost:5000`

## 📖 วิธีใช้งาน

1. **อัปโหลดรูป**: คลิกที่พื้นที่อัปโหลดหรือลากไฟล์มาวาง
2. **วิเคราะห์**: กดปุ่ม "🔍 วิเคราะห์"
3. **ดูผลลัพธ์**: ระบบจะแสดงผลว่าเป็น Normal หรือ Pneumonia พร้อม Confidence Score

## 📁 โครงสร้างโปรเจค

```
x-ray/
├── app.py                 # Flask backend
├── my_model.h5            # Trained model
├── requirements.txt       # Python dependencies
├── templates/
│   └── index.html        # Frontend UI
├── uploads/              # Uploaded images (auto-created)
└── README.md             # คู่มือนี้
```

## 🔧 การปรับแต่ง

### เปลี่ยนขนาดรูปที่ใช้ในการ predict

แก้ไขใน `app.py` ฟังก์ชัน `prepare_image()`:

```python
img = img.resize((224, 224))  # เปลี่ยนเป็นขนาดที่ model ของคุณต้องการ
```

### เปลี่ยน class names

แก้ไขใน `app.py`:

```python
classes = ['Normal', 'Pneumonia']  # เพิ่มหรือแก้ไข class ตามโมเดลของคุณ
```

### เปลี่ยน Port

แก้ไขใน `app.py`:

```python
app.run(debug=True, host='0.0.0.0', port=5000)  # เปลี่ยน port ตามต้องการ
```

## ⚠️ ข้อควรระวัง

- ⚕️ **ไม่ใช่เครื่องมือทางการแพทย์**: เว็บนี้เป็นเพียงเครื่องมือช่วยเหลือเบื้องต้น ไม่สามารถใช้ทดแทนการวินิจฉัยจากแพทย์ได้
- 🔒 **ความปลอดภัยของข้อมูล**: ระบบจะไม่เก็บรูปภาพที่อัปโหลด แต่หากจะใช้งานจริงควรเพิ่มระบบรักษาความปลอดภัย
- 📊 **Confidence Score**: คะแนนความมั่นใจไม่ได้หมายความว่าผลลัพธ์ถูกต้อง 100%

## 🛠️ Troubleshooting

### Import Error
ถ้าพบ error เกี่ยวกับ import:
```bash
pip install --upgrade tensorflow flask numpy Pillow
```

### Model Loading Error
ตรวจสอบว่าไฟล์ `my_model.h5` อยู่ในโฟลเดอร์เดียวกับ `app.py`

### Port Already in Use
เปลี่ยน port ในไฟล์ `app.py` หรือปิดโปรแกรมที่ใช้ port 5000 อยู่

## 📝 License

Free to use for educational purposes

## 👨‍💻 Developer

สร้างด้วย Flask, TensorFlow และ ❤️

---

**หมายเหตุ**: แอปพลิเคชันนี้สร้างขึ้นเพื่อการศึกษาและการสาธิต ไม่ควรนำไปใช้ในทางการแพทย์จริง
