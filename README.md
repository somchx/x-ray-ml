# 🫁 ระบบช่วยทำนายความผิดปกติจากภาพเอกซเรย์ทรวงอกด้วย Deep Learning

โครงงานพัฒนาระบบช่วยคัดกรองเบื้องต้นจากภาพเอกซเรย์ทรวงอก เพื่อจำแนกประเภทของโรค 4 ประเภท ได้แก่ COVID-19, ปกติ (NORMAL), ปอดอักเสบ (Pneumonia), และวัณโรค (Tuberculosis) โดยใช้เทคนิค Deep Learning

## 🎯 วัตถุประสงค์ของโครงงาน

1. **พัฒนาแบบจำลอง Deep Learning** สำหรับจำแนกภาพเอกซเรย์ทรวงอกออกเป็น 4 ประเภท:
   - **COVID19** - โรคติดเชื้อไวรัสโควิด-19
   - **NORMAL** - ปอดปกติไม่มีความผิดปกติ
   - **PNEU** - ปอดอักเสบ (Pneumonia)
   - **TURBER** - วัณโรค (Tuberculosis)

2. **ออกแบบกระบวนการเตรียมข้อมูล** ที่เหมาะสมกับภาพทางการแพทย์ รวมถึงการปรับขนาดภาพ (resize) และการทำ normalization

3. **สร้างหน้าเว็บ HTML interface** ที่เชื่อมต่อกับโมเดล ทำให้ผู้ใช้สามารถอัปโหลดภาพเอกซเรย์และรับผลทำนายได้สะดวก

4. **ศึกษาประสิทธิภาพของโมเดล** ผ่าน accuracy, precision, recall และ confusion matrix

## 📋 ที่มาและความสำคัญของปัญหา

โรคที่เกี่ยวกับระบบทางเดินหายใจ เช่น ปอดอักเสบ (Pneumonia), วัณโรค (Tuberculosis) และโรคที่เกิดจากเชื้อไวรัส เช่น COVID-19 เป็นโรคที่พบได้บ่อยและมีผลกระทบต่อสุขภาพของประชาชนอย่างมาก การวินิจฉัยโรคเหล่านี้มักอาศัยภาพเอกซเรย์ทรวงอก (Chest X-ray) และความชำนาญของรังสีแพทย์

อย่างไรก็ตาม ในสถานการณ์จริง:
- บุคลากรทางการแพทย์มีจำนวนจำกัด
- ภาพที่ต้อง่านต่อวันมีจำนวนมาก
- การทำงานอย่างเร่งรีบอาจทำให้เกิดความผิดพลาดได้

ด้วยเหตุนี้จึงมีแนวคิดในการใช้ Machine Learning / Deep Learning มาช่วย **"คัดกรองเบื้องต้น"** จากภาพเอกซเรย์ เพื่อช่วยให้แพทย์สามารถมองเห็นจุดที่น่าสงสัยได้เร็วขึ้น ลดภาระงาน และเพิ่มโอกาสในการวินิจฉัยและรักษาได้ทันท่วงที

> ⚠️ **หมายเหตุ**: โครงงานนี้เป็นเพียง **"ต้นแบบเพื่อการเรียนรู้"** ไม่สามารถนำไปใช้แทนการวินิจฉัยจริงทางการแพทย์ได้

## ✨ ฟีเจอร์หลัก

- 📤 อัปโหลดภาพเอกซเรย์ทรวงอก (รองรับ .jpg, .jpeg, .png)
- 🤖 วิเคราะห์ด้วย Deep Learning Model (CNN)
- 📊 แสดงผลการทำนาย 4 คลาส พร้อม Confidence Score
- 🎨 UI ที่สวยงามและใช้งานง่าย
- 🔄 รองรับ Drag & Drop

## � ชุดข้อมูล (Dataset)

โครงงานนี้ใช้ชุดข้อมูลจาก Kaggle:
- **Dataset Name**: [chest-xray-pneumoniacovid19tuberculosis](https://www.kaggle.com/datasets/jtiptj/chest-xray-pneumoniacovid19tuberculosis)
- **โครงสร้างข้อมูล**: แบ่งออกเป็น 3 ส่วนหลัก
  - `train/` - สำหรับเทรนโมเดล
  - `val/` - สำหรับ validation ระหว่างการเทรน
  - `test/` - สำหรับทดสอบโมเดลหลังเทรนเสร็จ

แต่ละโฟลเดอร์จะมีโฟลเดอร์ย่อยเป็นชื่อคลาส:
- `COVID19/` - ภาพเอกซเรย์ผู้ป่วยโควิด-19
- `NORMAL/` - ภาพเอกซเรย์ปอดปกติ
- `PNEU/` - ภาพเอกซเรย์ผู้ป่วยปอดอักเสบ
- `TURBER/` - ภาพเอกซเรย์ผู้ป่วยวัณโรค

## 🏗️ สถาปัตยกรรมโมเดล (Model Architecture)

โมเดลใช้สถาปัตยกรรมแบบ **Convolutional Neural Network (CNN)** ด้วย `tf.keras.Sequential()`:

### Feature Extraction Layers
```python
Conv2D(128, (5,5), activation='relu', input_shape=(224,224,1))
MaxPool2D(pool_size=(3,3))

Conv2D(64, (5,5), activation='relu')
MaxPool2D(pool_size=(3,3))

Conv2D(30, (3,3), activation='relu')
MaxPool2D(pool_size=(2,2))

Conv2D(30, (3,3), activation='relu')
MaxPool2D(pool_size=(2,2))
```

### Fully Connected Layers
```python
Flatten()
Dense(2048, activation='relu')
Dense(512, activation='relu')
Dropout(0.3)
Dense(256, activation='relu')
Dense(64, activation='relu')
Dropout(0.3)
Dense(32, activation='relu')
```

### Output Layer
```python
Dense(4, activation='softmax')  # 4 คลาส
```

### Model Compilation
```python
model.compile(
    loss='categorical_crossentropy',
    optimizer='adam',
    metrics=['accuracy']
)
```

## 🔧 การเตรียมข้อมูล (Data Preprocessing)

### การปรับขนาดภาพและการปรับสเกลค่าสี
```python
from tensorflow.keras.preprocessing.image import ImageDataGenerator

train_datagen = ImageDataGenerator(rescale=1./255)
val_datagen = ImageDataGenerator(rescale=1./255)
test_datagen = ImageDataGenerator(rescale=1./255)
```

### การโหลดข้อมูล
```python
train_generator = train_datagen.flow_from_directory(
    directory='path/train',
    target_size=(224, 224),
    color_mode="grayscale",      # ภาพ X-ray เป็นขาวดำ
    batch_size=32,
    class_mode="categorical",
    shuffle=True,
    seed=42
)
```

**พารามิเตอร์สำคัญ**:
- `target_size=(224, 224)` - ปรับขนาดภาพให้เท่ากันทั้งหมด
- `color_mode="grayscale"` - ภาพเอกซเรย์เป็นขาวดำ (1 channel)
- `rescale=1./255` - ปรับค่าพิกเซลจาก 0-255 ให้เป็น 0-1
- `class_mode="categorical"` - สำหรับการจำแนกหลายคลาส

## 🎓 การฝึกสอนโมเดล (Model Training)

```python
STEP_SIZE_TRAIN = train_generator.n // train_generator.batch_size
STEP_SIZE_VALID = val_generator.n // val_generator.batch_size

history = model.fit(
    train_generator,
    steps_per_epoch=STEP_SIZE_TRAIN,
    validation_data=val_generator,
    validation_steps=STEP_SIZE_VALID,
    epochs=30,
    verbose=1
)
```

ระหว่างการเทรนมีการบันทึกค่า:
- `loss` / `val_loss` - ค่า loss ของ train และ validation
- `accuracy` / `val_accuracy` - ค่า accuracy ของ train และ validation

สามารถนำค่าเหล่านี้ไปวาดกราฟการเรียนรู้ (learning curves) เพื่อตรวจสอบ overfitting

## 📈 การประเมินผล (Model Evaluation)

### 1. Validation Set Evaluation
```python
model.evaluate(val_generator, steps=STEP_SIZE_VALID)
```

### 2. Test Set Evaluation
```python
STEP_SIZE_TEST = test_generator.n // test_generator.batch_size
test_generator.reset()
pred = model.predict(test_generator, steps=STEP_SIZE_TEST, verbose=1)
predicted_class_indices = np.argmax(pred, axis=1)

# คำนวณ accuracy
accuracy = count_accurate / total_images
print(f"Test Accuracy: {round(accuracy*100, 2)}%")
```

### 3. Classification Report & Confusion Matrix
```python
from sklearn.metrics import classification_report, confusion_matrix

# แสดง precision, recall, f1-score รายคลาส
print(classification_report(actual_labels, predictions))

# สร้าง confusion matrix
cm = confusion_matrix(actual_labels, predictions)
sns.heatmap(cm, annot=True, fmt='g')
```

## 💾 การบันทึกและโหลดโมเดล

```python
# บันทึกโมเดล
model.save("ChestXRayModel.keras")

# โหลดและแปลงเป็น .h5
model = tf.keras.models.load_model("ChestXRayModel.keras")
model.save("ChestXRayModel.h5")
```



## 🚀 การติดตั้งและใช้งาน

### 1. ติดตั้ง Dependencies

```bash
pip install -r requirements.txt
```

หรือติดตั้งทีละตัว:

```bash
pip install flask tensorflow numpy Pillow werkzeug
```

### 2. เตรียม Model

ตรวจสอบว่ามีไฟล์ `ChestXRayModel.h5` อยู่ในโฟลเดอร์เดียวกับ `app.py`

### 3. เริ่มเซิร์ฟเวอร์

```bash
python app.py
```

### 4. เปิดเว็บบราวเซอร์

ไปที่: `http://localhost:5000`

## 📖 วิธีใช้งาน Web Interface

1. **อัปโหลดรูป**: คลิกที่พื้นที่อัปโหลดหรือลากไฟล์ภาพเอกซเรย์มาวาง
2. **วิเคราะห์**: กดปุ่ม "🔍 วิเคราะห์"
3. **ดูผลลัพธ์**: ระบบจะแสดงผลการทำนาย พร้อมระบุคลาสและ Confidence Score

**ผลลัพธ์ที่ได้**:
- **COVID19** - ตรวจพบสัญญาณของโควิด-19
- **NORMAL** - ปอดปกติไม่มีความผิดปกติ
- **PNEU** - ตรวจพบสัญญาณปอดอักเสบ
- **TURBER** - ตรวจพบสัญญาณวัณโรค

## 📁 โครงสร้างโปรเจค

```
x-ray/
├── app.py                       # Flask backend application
├── ChestXRayModel.h5            # Trained CNN model (4 classes)
├── ML_Chest_X_Ray_Project.ipynb # Jupyter Notebook สำหรับเทรนโมเดล
├── requirements.txt             # Python dependencies
├── templates/
│   └── index.html              # Frontend UI
├── uploads/                     # Uploaded images (auto-created)
└── README.md                    # เอกสารนี้
```

## 🔧 การปรับแต่ง

### เปลี่ยนชื่อไฟล์โมเดล

แก้ไขใน `app.py`:

```python
model = load_model('ChestXRayModel.h5')  # เปลี่ยนเป็นชื่อไฟล์ของคุณ
```

### เปลี่ยนขนาดรูปที่ใช้ในการ predict

แก้ไขใน `app.py` ฟังก์ชัน `prepare_image()`:

```python
img = img.resize((224, 224))  # ขนาดต้องตรงกับที่โมเดลเทรน
```

### เปลี่ยน class names

แก้ไขใน `app.py`:

```python
classes = ['COVID19', 'NORMAL', 'PNEU', 'TURBER']  # 4 คลาส
```

### เปลี่ยน Port

แก้ไขใน `app.py`:

```python
app.run(debug=True, host='0.0.0.0', port=5000)  # เปลี่ยน port ตามต้องการ
```

## 📊 ผลการทดลอง

### ประสิทธิภาพของโมเดล

โมเดล CNN ที่พัฒนาขึ้นสามารถจำแนกภาพเอกซเรย์ทรวงอก 4 คลาสได้ในระดับที่น่าพอใจ:

- **Architecture**: Custom CNN with 4 Conv2D layers + Dense layers
- **Input Size**: 224×224 pixels (Grayscale)
- **Training Epochs**: 30 epochs
- **Optimizer**: Adam
- **Loss Function**: Categorical Crossentropy

### ข้อสังเกตจากการทดลอง

✅ **จุดแข็ง**:
- โมเดลสามารถเรียนรู้จากชุดข้อมูลเอกซเรย์ทรวงอก 4 คลาสได้ดี
- การเตรียมข้อมูลด้วย grayscale และ rescaling ช่วยให้โมเดลเรียนรู้ได้สม่ำเสมอ
- การใช้ Dropout ช่วยลด overfitting

⚠️ **ข้อจำกัด**:
- บางคลาสอาจมีการสับสน เช่น PNEU กับ TURBER
- ประสิทธิภาพขึ้นอยู่กับคุณภาพและมุมมองของภาพเอกซเรย์
- จำเป็นต้องมีข้อมูลเทรนที่หลากหลายเพียงพอ

### เครื่องมือประเมินผล

- **Classification Report**: แสดง precision, recall, f1-score รายคลาส
- **Confusion Matrix**: แสดงการจำแนกถูก/ผิดระหว่างคลาส
- **Learning Curves**: แสดงแนวโน้มการเรียนรู้และ overfitting

## ⚠️ ข้อควรระวังและข้อจำกัด

### ⚕️ ไม่ใช่เครื่องมือทางการแพทย์

เว็บแอปพลิเคชันนี้เป็นเพียง **"ระบบช่วยคัดกรองเบื้องต้น"** และ **"ต้นแบบเพื่อการเรียนรู้"** เท่านั้น:

- ❌ **ไม่สามารถใช้ทดแทน**การวินิจฉัยจากแพทย์ผู้เชี่ยวชาญ
- ❌ **ไม่ควรนำไปใช้**ในการตัดสินใจทางการแพทย์จริง
- ✅ **ควรใช้เป็น**เครื่องมือช่วยเหลือเบื้องต้นเพื่อลดภาระงานของบุคลากรทางการแพทย์
- ✅ **ผลลัพธ์ทุกครั้ง**ต้องได้รับการตรวจสอบโดยรังสีแพทย์หรือแพทย์ผู้เชี่ยวชาญ

### 🔒 ความปลอดภัยของข้อมูล

- ระบบจะไม่เก็บรูปภาพที่อัปโหลดถาวร (เก็บชั่วคราวในโฟลเดอร์ `uploads/`)
- หากจะนำไปใช้งานจริง ควรเพิ่ม:
  - ระบบ Authentication และ Authorization
  - การเข้ารหัสข้อมูล (Encryption)
  - การจัดการความเป็นส่วนตัวตาม PDPA/GDPR

### 📊 Confidence Score

- คะแนนความมั่นใจ (Confidence Score) **ไม่ได้หมายความว่า**ผลลัพธ์ถูกต้อง 100%
- โมเดลอาจมี bias จากชุดข้อมูลที่ใช้เทรน
- คุณภาพของภาพอินพุตมีผลต่อความแม่นยำของการทำนาย

### 🎯 ข้อจำกัดของระบบ

- โมเดลเทรนด้วยข้อมูลจำกัด อาจไม่ครอบคลุมทุกกรณี
- ภาพที่มีมุมมองผิดปกติหรือคุณภาพต่ำอาจให้ผลลัพธ์ที่คลาดเคลื่อน
- ไม่สามารถตรวจจับโรคอื่นๆ นอกเหนือจาก 4 คลาสที่เทรนไว้

## 🛠️ Troubleshooting

### Import Error
ถ้าพบ error เกี่ยวกับ import:
```bash
pip install --upgrade tensorflow flask numpy Pillow
```

### Model Loading Error
ตรวจสอบว่า:
- ไฟล์ `ChestXRayModel.h5` อยู่ในโฟลเดอร์เดียวกับ `app.py`
- เวอร์ชัน TensorFlow/Keras ที่ใช้ compatible กับโมเดล

### Port Already in Use
```bash
# เปลี่ยน port ในไฟล์ app.py หรือ
# หา process ที่ใช้ port 5000
lsof -i :5000
# ปิด process นั้น
kill -9 <PID>
```

### ภาพไม่แสดงผล หรือ Prediction Error
ตรวจสอบว่า:
- ไฟล์ภาพเป็นนามสกุล .jpg, .jpeg หรือ .png
- ขนาดไฟล์ไม่ใหญ่เกินไป
- ภาพเป็นภาพเอกซเรย์ทรวงอก (Chest X-ray) จริง

## � การพัฒนาต่อยอด

### แนวทางปรับปรุงโมเดล

1. **เพิ่มข้อมูลเทรน**: รวบรวมภาพเอกซเรย์เพิ่มเติมเพื่อความหลากหลาย
2. **Data Augmentation**: หมุน, flip, zoom ภาพเพื่อเพิ่มความแกร่งของโมเดล
3. **Transfer Learning**: ใช้โมเดล pre-trained เช่น ResNet, EfficientNet
4. **Ensemble Methods**: รวมผลทำนายจากหลายโมเดล
5. **Class Balancing**: จัดการข้อมูลที่ไม่สมดุลระหว่างคลาส

### แนวทางพัฒนาระบบ

1. **เพิ่ม Grad-CAM**: แสดงภาพ heatmap บริเวณที่โมเดลให้ความสำคัญ
2. **RESTful API**: สร้าง API endpoint สำหรับการ integrate กับระบบอื่น
3. **Database Integration**: เก็บประวัติการทำนายและผลลัพธ์
4. **Multi-language Support**: รองรับหลายภาษา
5. **Mobile Application**: พัฒนาแอปมือถือสำหรับใช้งานในสนาม

## 🎓 เอกสารอ้างอิง

### Dataset
- [Chest X-Ray Pneumonia COVID19 Tuberculosis Dataset (Kaggle)](https://www.kaggle.com/datasets/jtiptj/chest-xray-pneumoniacovid19tuberculosis)

### Technologies Used
- **Python 3.x**
- **TensorFlow/Keras** - Deep Learning Framework
- **Flask** - Web Framework
- **NumPy** - Numerical Computing
- **Pillow (PIL)** - Image Processing
- **scikit-learn** - Model Evaluation Metrics

## � สรุป

โครงงานนี้แสดงให้เห็นถึงศักยภาพของ Deep Learning ในการช่วยคัดกรองเบื้องต้นจากภาพเอกซเรย์ทรวงอก โดย:

✅ พัฒนาโมเดล CNN ที่สามารถจำแนก 4 คลาส (COVID19, NORMAL, PNEU, TURBER)  
✅ ออกแบบกระบวนการเตรียมข้อมูลที่เหมาะสมกับภาพทางการแพทย์  
✅ สร้าง Web Interface ที่ใช้งานง่ายสำหรับผู้ใช้ทั่วไป  
✅ ประเมินผลด้วยเครื่องมือมาตรฐาน (accuracy, confusion matrix, classification report)  

ระบบนี้สามารถเป็น **ต้นแบบ** สำหรับการพัฒนาต่อยอดไปสู่ระบบช่วยเหลือทางการแพทย์ที่แท้จริง แต่ยังคงต้องผ่านการทดสอบ วิจัย และรับรองมาตรฐานทางการแพทย์เพิ่มเติม

---

## 👨‍💻 ผู้พัฒนา

พัฒนาด้วย Python, TensorFlow, Flask และ ❤️

**สร้างขึ้นเพื่อการศึกษาและการสาธิต**

---

## 🧾 จัดทำโดย

- รหัสนักศึกษา: **6710421004**
- ชื่อ: **ชนัญญา เอี่ยมประโคน**

## รายวิชา

- **CI7101 Artificial Intelligence 1-68**

## สถาบันการศึกษา

- National Institute of Development Administration (NIDA)
- สถาบันบัณฑิตพัฒนบริหารศาสตร์

## คณะ

- คณะสถิติประยุกต์

## สาขาวิชา

- วิทยาการคอมพิวเตอร์และระบบสารสนเทศ (CSIS)
- Computer Science and Information Systems

---

**© 2025 - Chest X-Ray Classification System**  
*Machine Learning Project for Educational Purposes*
